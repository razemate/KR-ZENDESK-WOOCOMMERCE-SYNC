### **Final Plan: The "Isolated V2" Strategy**

I understand completely. To ensure **zero risk** to your current app, we will not touch the existing database table **OR** the existing code. We will build a completely separate "V2" system alongside it.

#### **1. The Strategy: Complete Isolation**
- **Existing System (V1)**: `woo_subscription_snapshot` table, `webhook-woo.js`, `sync-initial.js`. **Left 100% untouched.**
- **New System (V2)**: `customer_unified_snapshot` table, `webhook-v2.js`, `sync-v2.js`. **Brand new.**

#### **2. Implementation Steps**

**Step 1: Create New Table (`SUPABASE_V2.sql`)**
I will create a SQL file to:
- Create the new `customer_unified_snapshot` table with the advanced schema (Composite Key, JSONB columns, Source indicator).
- *Optionally* copy data from the old table to the new one as a starting point (Read-Only copy).

**Step 2: Create New Endpoints**
I will create two **new** files:
1.  `api/sync-v2.js`: A script to manually fetch all subscriptions and populate the **new** table.
2.  `api/webhook-v2.js`: A new webhook handler that listens for WooCommerce events and updates the **new** table.

**Step 3: User Action (Add New Webhooks)**
Since we are not touching the old webhooks (to keep the old app safe), you will need to **add new webhooks** in WooCommerce:
- **Topic**: `Subscription Updated`, `Order Created`, etc. (Same topics).
- **Delivery URL**: Your Vercel URL + `/api/webhook-v2`.
- **Secret**: Same secret.

**Result**:
- Your Zendesk App continues running on "V1" (Old Table, Old Webhook) as if nothing happened.
- Your New App will run on "V2" (New Table, New Webhook).
- Both tables will be updated automatically in parallel.

This meets your requirement to **not touch or edit** the current table/setup at all.

Ready to build the V2 system?
