I will update the app to use the new Supabase table and prepare it for deployment.

**1. Codebase Updates**

* **Table Rename**: I will replace all references to `customer_unified_view` with `woo_subscription_snapshot` in the following files:

  * `api/read.js`

  * `api/refresh-single.js`

  * `api/webhook-woo.js`

  * `api/cron-nightly.js`

  * `api/sync-initial.js`

  * `api/export-recent.js`

  * `scripts/manual-sync-all.js`

* **Version Bump**: I will update `manifest.json` version to `1.0.24`.

**2. Packaging for Zendesk**

* I will create a zip file named `KR_Zendesk_Woo_App_v24.zip` containing the `assets`, `manifest.json`, and `translations` folders.

* I will place this zip in `ZENDESK_WOO_SYNC/UPLOAD_READY`.

**3. Deployment**

* **GitHub**: I will commit and push the changes to the repository.

* **Vercel**: I will deploy the app to production using `vercel --prod`.

* **Environment Variables**: I have detected that `SUPABASE_SERVICE_ROLE_KEY` is missing from your Vercel environment variables, which caused the 500 error (masked as 404 by the client). I will fix the code first, but you **MUST** add this key to your V

