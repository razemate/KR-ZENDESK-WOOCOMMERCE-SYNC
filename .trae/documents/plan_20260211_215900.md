I have identified the new table name as `woo_subscriptions` from the Supabase schema. This table has a different structure than the previous one and appears to be managed by an external process (likely WP Fusion), as it contains fields like `wp_fusion_contact_id`.

I will update the app to **READ** from this new table and disable the conflicting write logic (syncs), as we should not overwrite data managed by another tool.

**1. Update `api/read.js` (The Fix)**
*   Query `woo_subscriptions` instead of `woo_subscription_snapshot`.
*   Map the new columns (`first_name`, `status`, `total`, etc.) to the format the Zendesk frontend expects.
*   *Note*: The new table does not store "Latest Order" data, so this section will be empty in the cached view. It will still populate if the user clicks "Refresh".

**2. Update `api/refresh-single.js`**
*   Keep the logic that fetches fresh data from WooCommerce (including Latest Order).
*   **Remove** the Supabase `upsert` call (to avoid schema errors or conflicts).
*   The "Refresh" button will now just show live data without saving it to the database.

**3. Disable Write Operations**
*   **`api/webhook-woo.js`**: Update to receive webhooks but **skip** writing to Supabase.
*   **`api/cron-nightly.js`**: Disable the nightly sync task.
*   **`api/export-recent.js`**: Update to export from `woo_subscriptions`.

**4. Deployment**
*   Bump version to `1.0.25`.
*   Create `KR_Zendesk_Woo_App_v25.zip`.
*   Push to GitHub and Deploy to Vercel.

**Important**: You must ensure `SUPABASE_SERVICE_ROLE_KEY` is added to Vercel environment variables if you haven't already, although `read.js` typically uses the standard key. The error you saw confirms the connection works but the table was missing.