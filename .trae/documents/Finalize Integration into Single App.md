I give you my **100% assurance** that the **Zendesk-Woo Sync app will remain safe and fully functional**. I will meticulously revert *only* the specific AI-related lines I added, leaving the original WooCommerce logic, Supabase connections, and Vercel configuration untouched.

Here is the safe execution plan:

### **Phase 1: Secure Migration (Preserving Your New Features)**
I will extract the latest "AI Assistant" features we built (Settings, Custom Instructions) and move them to your new dedicated folder: `c:\Users\Chris Saulon\Desktop\APPS by CHRIS\BUILT\KR ZENDESK AI ASSISTANT`.
1.  **Create App Structure**: Set up the `assets` and `api` folders in the new location.
2.  **Migrate Code**: Copy the *latest* AI logic (including the new "Custom Instructions" and "Settings" panel) from the Woo app into the new standalone app files.
3.  **Create Manifest**: Generate a `manifest.json` for the AI Assistant so it is recognized as a valid Zendesk app.

### **Phase 2: Restoration (Cleaning the Woo App)**
Once the AI code is safely in the new folder, I will carefully clean up the **Zendesk-Woo Sync** app:
1.  **`iframe.html`**: Remove the "AI Assistant" HTML section.
2.  **`main.js`**: Remove the `initAI()` function and logic.
3.  **`app-styles.css`**: Remove the AI-specific CSS.
4.  **Result**: The Woo Sync app will be restored exactly to its previous state—pure, lightweight, and stable.

### **Phase 3: Verification**
I will verify that:
1.  The **Zendesk-Woo Sync** folder contains *only* Woo-related code.
2.  The **AI Assistant** folder contains the full, standalone AI app.

You will end up with two clean, separate folders ready for upload.

**Please confirm if you are ready for me to execute this safe separation.**